{
  "name": "bluefood-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix be && npm install --prefix fe/admin && npm install --prefix fe/mobile/app",
    "dev:be": "npm run dev --prefix be",
    "dev:admin": "npm run dev --prefix fe/admin",
    "dev:mobile": "npm run dev --prefix fe/mobile/app",
    "build": "npm run typecheck --prefix be && npm run build --prefix fe/admin && npm run build --prefix fe/mobile/app",
    "build:be": "npm run typecheck --prefix be",
    "build:admin": "npm run build --prefix fe/admin",
    "build:mobile": "npm run build --prefix fe/mobile/app",
    "prisma:generate": "npm run prisma:generate --prefix be",
    "prisma:migrate": "npm run prisma:migrate --prefix be"
  }
}
